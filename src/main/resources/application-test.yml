spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:template_test_db;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: false

management:
  tracing:
    enabled: false
